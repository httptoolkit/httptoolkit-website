import { StyledCheckIcon } from './check-icon.styles';
import { Check } from '../icon';

export const CheckIcon = () => {
  return (
    <StyledCheckIcon>
      <Check size={12} />
    </StyledCheckIcon>
  );
};
