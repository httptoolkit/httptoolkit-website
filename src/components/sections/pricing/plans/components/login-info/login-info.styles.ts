'use client';

import { styled } from '@/styles';

export const StyledLoginInfoWrapper = styled.div`
  display: grid;
  justify-content: center;
  gap: 16px;

  *[data-button='true'] {
    margin: 0 auto;
  }
`;
